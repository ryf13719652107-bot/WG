# 智能对冲马丁机器人 - 启动与停止教程

***

## 启动机器人

### 方法一：手动启动（推荐）

#### 步骤 1：启动后端服务器

打开 **PowerShell** 或 **终端**，执行：

```powershell
cd C:\Users\86137\Desktop\智能对冲马丁\backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

#### 步骤 2：启动前端界面

**新开一个终端窗口**，执行：

```powershell
cd C:\Users\86137\Desktop\智能对冲马丁\frontend
npm run dev
```

如果终端卡死或无法响应，打开新终端执行：

```powershell
# 停止所有 Python 进程（后端）
taskkill /f /im python.exe

# 停止所有 Node 进程（前端）
taskkill /f /im node.exe
```

***

| <br /> |
| ------ |
| <br /> |

# 终端1 — 启动后端

```
cd c:\Users\86137\Desktop\对冲网格马丁\backend
.venv\Scripts\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

# 终端2 — 启动前端 (可选，如需改UI)

```
cd c:\Users\86137\Desktop\对冲网格马丁\frontend
npm run dev
```

***

## 更新代码

### Windows 本地更新

```powershell
cd C:\Users\86137\Desktop\智能对冲马丁
git pull origin main
```

如果本地有修改，先暂存再拉取：

```powershell
cd C:\Users\86137\Desktop\智能对冲马丁
git stash
git pull origin main
git stash pop
```

拉取后重启后端和前端即可。

### Linux 服务器更新

改前端

cd /root/WG

git pull origin main

cd /root/MD/frontend

npm install

npm run build

公开拉取并重启策略

```
cd /root/WG
git pull https://github.com/ryf13719652107-bot/WG.git main
pkill -f uvicorn
cd backend
PYTHONPATH=. nohup python3.11 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /root/MD/backend.log 2>&1 &

```

```bash
cd /root/MD
git pull origin main
```

更新后重启后端：

```bash
cd /root/MD/backend
# 先停止旧进程
pkill -f "uvicorn app.main" || true
# 重新启动
nohup python3.11 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /root/MD/backend.log 2>&1 &
```

### 推送本地修改到 GitHub

```powershell
cd C:\Users\86137\Desktop\智能对冲马丁
git add -A
git commit -m "描述你的修改内容"
git push origin main
```

推送后去服务器 `git pull` 即可同步。

***

## 常见问题

### Q1: 端口 8000 被占用

**错误信息**：

```
[WinError 10048] 通常每个套接字地址(协议/网络地址/端口)只允许使用一次
```

**解决方法**：

```powershell
# 查找占用 8000 端口的进程
netstat -ano | findstr :8000

# 停止该进程（将 xxxx 替换为实际的 PID）
taskkill /PID xxxx /F
```

### Q2: 前端无法连接后端

**现象**：页面显示 "未连接"

**解决方法**：

1. 确认后端已启动（看到 `Uvicorn running` 信息）
2. 检查防火墙是否阻止了 8000 端口
3. 刷新浏览器页面（F5）

### Q3: 余额获取失败

**可能原因**：

- API Key 无效
- 选择的是测试网但使用了主网 API Key
- 网络连接问题

**解决方法**：

1. 检查 API Key 是否正确
2. 在状态栏切换账户（如果有多个账户）
3. 检查 `.env` 文件中的 `BINANCE_TESTNET` 设置

### Q4: 如何切换测试网/主网

**方法一**：在系统设置中添加不同账户

- 测试网账户：勾选"测试网"选项
- 主网账户：不勾选"测试网"选项

**方法二**：修改全局配置
编辑 `backend/.env` 文件：

```
# 使用测试网
BINANCE_TESTNET=true

# 使用主网
BINANCE_TESTNET=false
```

***

## 命令速查表

### 启动命令

```powershell
# 后端
cd C:\Users\86137\Desktop\智能对冲马丁\backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端
cd C:\Users\86137\Desktop\智能对冲马丁\frontend
npm run dev

# 一键启动
cd C:\Users\86137\Desktop\智能对冲马丁
.\start_bot.ps1
```

### 停止命令

```powershell
# 停止 Python（后端）
taskkill /f /im python.exe

# 停止 Node（前端）
taskkill /f /im node.exe

# 查看进程
tasklist | findstr python
tasklist | findstr node
```

### 调试命令

```powershell
# 测试主网数据
cd C:\Users\86137\Desktop\智能对冲马丁\backend
python test_mainnet.py

# 检查端口占用
netstat -ano | findstr :8000
netstat -ano | findstr :5173
```

***

## 目录结构

```
智能对冲马丁/
├── backend/          # 后端代码
│   ├── app/          # 主应用
│   ├── data/         # 数据库文件
│   └── .env          # 环境配置
├── frontend/         # 前端代码
│   ├── src/          # 源代码
│   └── dist/         # 构建输出
├── start_bot.ps1     # 一键启动脚本
└── 启动停止教程.md   # 本教程
```

***

## 技术支持

如有问题，请检查：

1. 后端日志输出
2. 浏览器开发者工具（F12）网络请求
3. 确认 API Key 权限（需要合约交易权限）

***

**最后更新**：2026-05-06（新增获取API Key教程、加密密钥自动生成、多账户支持）
