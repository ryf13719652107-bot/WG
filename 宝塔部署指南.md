# 宝塔面板部署指南（马丁网格机器人）

适用于：**Linux + 宝塔**，示例服务器：`119.28.188.116`。  
生产环境：**仅需一个端口 `8000`** — FastAPI 同时托管接口与前端静态页（`frontend/dist`），**不必**再单独跑 `vite preview`。

仓库：<https://github.com/ryf13719652107-bot/WG>

---

## 一、宝塔里先装好依赖

在 **软件商店** 安装（版本选主流即可）：

| 软件 | 用途 |
|------|------|
| **Nginx** | 反向代理、HTTPS（推荐） |
| **Git** | 拉代码 |

系统自带或额外安装：

- **Python 3.11+**（若没有：`Python 项目管理器` 或系统包 `python3.11`、`python3.11-venv`）
- **Node.js 20 LTS+**（前端 Tailwind v4 / Vite 6 要求 **≥20**，仅用 18 会触发 `EBADENGINE` 与 `@tailwindcss/oxide` 原生绑定失败）

---

## 二、放行端口与安全组

1. **云厂商控制台**（腾讯云 / 阿里云等）：安全组放行  
   - **8000**（若浏览器直连 IP:8000），或  
   - **80 / 443**（若只用 Nginx 反代，可不对外开放 8000）。
2. **宝塔 → 安全**：同样放行对应端口。

---

## 三、克隆代码（建议目录）

SSH 登录服务器，或使用宝塔 **终端**：

```bash
cd /www/wwwroot
git clone https://github.com/ryf13719652107-bot/WG.git
cd WG
```

以后更新：

```bash
cd /www/wwwroot/WG
git pull origin main
```

---

## 四、后端虚拟环境与数据库迁移

```bash
cd /www/wwwroot/WG/backend

python3 -m venv .venv
source .venv/bin/activate   # 若默认是 python3.11，也可用 python3.11 -m venv .venv

pip install -U pip
pip install -r requirements.txt

mkdir -p data logs
```

**数据库**：默认 SQLite，路径见 `backend/app/config.py`（`data/trading_bot.db`）。首次迁移：

```bash
cd /www/wwwroot/WG/backend
source .venv/bin/activate
alembic upgrade head
```

应用启动时仍会执行 `init_db()`，与 Alembic 配合即可。

---

## 五、前端构建（打包进后端）

```bash
cd /www/wwwroot/WG/frontend
npm install
npm run build
```

构建成功后必须有目录：`WG/frontend/dist`。后端会从该目录挂载静态资源。

---

## 六、环境变量（强烈建议）

在 **`/www/wwwroot/WG/backend/.env`** 新建或编辑（不要提交到 Git）：

```env
# 网页登录密码（生产务必设置；对应 Settings.web_ui_password）
WEB_UI_PASSWORD=你的强密码

# 可选：加密密钥（账户 API 密文等）
# ENCRYPTION_KEY=

# 可选：数据库（默认 SQLite 可不写）
# DATABASE_URL=sqlite+aiosqlite:///data/trading_bot.db

# 可选：与前端访问域名一致，便于浏览器跨域（若只通过同域 Nginx 访问可省略）
# CORS_ORIGINS=http://119.28.188.116,http://119.28.188.116:8000,https://你的域名
```

保存后重启后端进程生效。

---

## 七、启动后端（生产）

必须在 **`backend` 目录** 下启动（日志、`data`、`alembic` 路径一致）：

```bash
cd /www/wwwroot/WG/backend
source .venv/bin/activate

# 前台调试（SSH 断开即停）
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**后台常驻（简单写法）：**

```bash
cd /www/wwwroot/WG/backend
source .venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 \
  >> logs/server.log 2>&1 &
```

浏览器访问：**`http://119.28.188.116:8000`**（若已设 `WEB_UI_PASSWORD`，按页面提示登录）。

停止：

```bash
pkill -f "uvicorn app.main:app" || true
```

---

## 八、宝塔「进程守护」（Supervisor）或 systemd（推荐常驻）

你用 **进程守护** 跑是对的：崩溃会自动拉起，比 `nohup` 省心。

### 方案 A：宝塔进程守护（Supervisor）

1. **软件商店** 搜索安装 **Supervisor 管理器**（部分版本显示为「Supervisor」或「进程守护」）。  
2. 打开 **Supervisor → 添加守护进程**，建议填写：

| 项 | 填写示例 |
|----|----------|
| **名称** | `martin-grid`（任意英文字符） |
| **启动用户** | `root`（若改 `www`，须保证 `backend/data`、`backend/logs` 可写） |
| **运行目录** | `/www/wwwroot/WG/backend` |
| **启动命令** | `/www/wwwroot/WG/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000` |

3. **不要**再用 `nohup` 或手动再起一条 uvicorn，否则会出现 **两个进程抢 8000 端口** 或策略重复调度。添加守护进程前可先执行：  
   `pkill -f "uvicorn app.main:app" || true`  
4. **环境变量**：若在面板里没有 `.env` 加载选项，可把 `WEB_UI_PASSWORD` 等写在 **守护进程 → 环境变量**（若有），或在 **`backend/.env`** 里配置（程序会读该文件）。  
5. 保存后 **启动**，状态应为 **RUNNING**；日志可在 Supervisor 里看 stdout/stderr，或继续看 **`backend/logs/`**（如 `bot.log`、`server.log`）。  
6. **更新代码后**：在宝塔里对该进程点 **重启**，或在终端 `supervisorctl restart martin-grid`（名称以你填写为准）。

若面板要求「启动命令」必须用 `bash -c`，可用：

```bash
bash -lc 'cd /www/wwwroot/WG/backend && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000'
```

此时「运行目录」仍可填 `/www/wwwroot/WG/backend`，或以面板实际选项为准。

### 方案 B：systemd（示例）

创建 `/etc/systemd/system/trading-bot.service`：

```ini
[Unit]
Description=Martin Grid Trading Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/www/wwwroot/WG/backend
Environment="PATH=/www/wwwroot/WG/backend/.venv/bin:/usr/local/bin:/usr/bin"
ExecStart=/www/wwwroot/WG/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload
systemctl enable trading-bot
systemctl start trading-bot
systemctl status trading-bot
```

---

## 九、Nginx 反向代理（可选，推荐域名 + HTTPS）

宝塔 **网站 → 添加站点**（纯静态或任意 PHP 占位均可），然后 **设置 → 反向代理**：

- **代理名称**：`api`
- **目标 URL**：`http://127.0.0.1:8000`
- **发送域名**：`$host`

或使用「配置文件」片段：

```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400;
}
```

WebSocket（仪表盘等）如需额外配置，可把 `Upgrade` 头补上（宝塔新版反向代理常自带）。

启用 **SSL** 后，访问 **`https://你的域名`** 即可。

---

## 十、与仓库 `deploy.sh` 的差异说明

根目录 `deploy.sh` 里仍会启动 **`vite preview :5173`**。  
**生产环境不必执行这一段**：本项目已在 **`8000`** 上集成前端 **`dist`**，按本文 **第五节 + 第七节** 即可。

你若只想一键更新，可自行改成：**`git pull` → `pip install` → `npm run build` → 重启 uvicorn**，不要用脚本里的 5173。

---

## 十一、部署检查清单

- [ ] `frontend/dist` 存在且 `npm run build` 无报错  
- [ ] `backend/data`、`backend/logs` 可写  
- [ ] `alembic upgrade head` 已执行（或依赖启动时迁移）  
- [ ] 已设置 **`WEB_UI_PASSWORD`**（生产）  
- [ ] 云安全组 + 宝塔防火墙端口一致  
- [ ] 使用进程守护时 **仅保留一条** uvicorn 进程（更新代码后在面板里重启守护进程）  

---

## 十二、常见问题

**打不开页面**：检查 uvicorn 是否监听 `0.0.0.0:8000`，以及防火墙 / 安全组。  

**静态页 404**：确认在 **`backend`** 目录启动，且 `frontend/dist` 相对仓库路径正确。  

**进程守护里反复重启**：看 Supervisor 日志是否端口占用（重复启动了多条 uvicorn）；先停守护进程再 `pkill` 清理后再启动。  

---

部署完成后，控制台地址示例：**`http://119.28.188.116:8000`**（或使用你已绑定域名 + HTTPS）。
